extends layout

block content
  h1= title
  p Welcome to #{title}
  form(name="add-estimation", method="post")
    div.input
      span.label Nom du Site :
      input(type="text", name="title")
    div.actions
      input(type="submit", value="Create Site" class="btn btn-primary")
